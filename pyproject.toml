[tool.poetry]
name = "MOOweather"
version = "0.1.0"
description = "MOOweather: CLI weather application"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"
requests = "^2.31.0"

[tool.poetry.scripts]
mooweather = "src.main:main"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"